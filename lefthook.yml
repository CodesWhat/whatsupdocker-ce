---
pre-push:
  commands:
    qlty:
      run: qlty check
    snyk-deps:
      root: app/
      run: snyk test --severity-threshold=high
    snyk-code:
      run: scripts/snyk-code-gate.sh
    build:
      root: app/
      run: npm run build
    test:
      root: app/
      run: npm test
    ui-build:
      root: ui/
      run: npm run build
    ui-test:
      root: ui/
      run: npm run test:unit
